# AI 客服三大功能逐帧检查报告（2026-07-19）

> **检查目标**：逐帧审查 AI 客服功能、改备注功能、识别买家询问的商品功能是否完整、代码是否完整。
>
> **检查方法**：3 个并行 Explore agent + 主代理交叉验证（对 agent 误报项亲自读取源码确认）。
>
> **结论**：全部 21 个维度（8 + 6 + 7）通过 ✅，代码完整、功能完整。

---

## 一、AI 客服功能完整性审查（8 维度）

### 维度 1：消息接收链路 — ✅ 完整

| 检查项 | 状态 | 证据 |
|--------|------|------|
| 检查 `shopConfig.autoReply` 状态（false 则 return） | ✅ | [ShopSupervisor.ts:915-1050](file:///d:/code/AIkefu/src/shop/ShopSupervisor.ts#L915) `handleIncomingMessage` 入口 |
| 消息去重 | ✅ | `processedMessageHashes` 实现 |
| 行为追踪 | ✅ | `behaviorTracker?.trackMessageReceived()` |
| `processingSessions` 会话锁 | ✅ | 同会话同时只处理一条消息 |
| `sessionCooldowns` 15 秒冷却期 | ✅ | 防止回复风暴 |

### 维度 2：7 级回复生成管线 — ✅ 完整

**位置**：[ShopSupervisor.ts:1415-1765](file:///d:/code/AIkefu/src/shop/ShopSupervisor.ts#L1415) `generateReply` 方法

| 步骤 | 状态 | 说明 |
|------|------|------|
| 1. 精确缓存查询 | ✅ | `cache.get()` 命中直接返回 |
| 2. 语义缓存查询 | ✅ | `cache.getSemantic()` 模糊匹配 |
| 3. 意图评估 + LLM 二次验证 | ✅ | `llmIntentRecognizer.recognize()` → ModelGateway.route() |
| 4. 规则引擎匹配 | ✅ | `ruleEngine.match(text)` |
| 5. 学习模式匹配 | ✅ | `learnedPatterns` 历史问答复用 |
| 6. 商品/订单/推荐上下文注入 | ✅ | L1533-1609 `productMatcher.match()` + `detectOrderInfo()` + `conversionEngine.recommend()` |
| 7. Multi-Agent 决策 | ✅ | `agentRouter.route()` 场景路由 + AgentContext 注入 |
| 8. 统一敏感词检查 | ✅ | `sensitiveChecker.check()` block/warn/replace |
| 9. 审计日志写入 | ✅ | `auditLogger.log()` 记录完整请求/回复 |
| 10. 缓存写入 | ✅ | `cache.set()` 持久化 |

### 维度 3：视觉轮询（runVisualPoll） — ✅ 完整

**位置**：[ShopSupervisor.ts:2296-2410](file:///d:/code/AIkefu/src/shop/ShopSupervisor.ts#L2296)

| 检查项 | 状态 |
|--------|------|
| autoReply 状态检查 | ✅ |
| vision/OCR + DOM 双模式 | ✅ |
| replyGuard 陈旧检查（isReplyGuardStale） | ✅ |
| 错误降级到 DOM 轮询兜底 | ✅ |

### 维度 4：缓存层 — ✅ 完整

**位置**：[src/cache/LruCache.ts:18-202](file:///d:/code/AIkefu/src/cache/LruCache.ts#L18)

| 检查项 | 状态 |
|--------|------|
| 精确缓存 get/set | ✅ |
| 语义缓存 getSemantic | ✅ |
| TTL 机制 | ✅ |
| max_entries_per_shop 限制 | ✅ |
| max_entries_global 限制 | ✅ |

### 维度 5：敏感词 — ✅ 完整

**位置**：[src/secrets/SensitiveWordChecker.ts:85-111](file:///d:/code/AIkefu/src/secrets/SensitiveWordChecker.ts#L85) + [config/dict/sensitive-words.txt](file:///d:/code/AIkefu/config/dict/sensitive-words.txt)

| 检查项 | 状态 |
|--------|------|
| block 动作（拦截返回 fallback_response） | ✅ |
| warn 动作（告警不阻断） | ✅ |
| replace 动作（替换为 `***`） | ✅ |
| "微信"、"支付宝"、"线下" 为 block 类型 | ✅ |
| 总词数约 145 条 | ✅ |

### 维度 6：转人工 — ✅ 完整

**位置**：[ShopSupervisor.ts:1357-1413](file:///d:/code/AIkefu/src/shop/ShopSupervisor.ts#L1357) `executeTransfer` 方法

| 检查项 | 状态 |
|--------|------|
| transfer:success / transfer:failed 事件触发 | ✅ |
| ShopInstance 继承 EventEmitter | ✅ |
| 转接执行 8 步页面操作 | ✅ |
| ManualMode 切换 | ✅ |

### 维度 7：DeepSeek 客户端 — ✅ 完整

**位置**：[src/deepseek/DeepSeekClient.ts](file:///d:/code/AIkefu/src/deepseek/DeepSeekClient.ts)

| 检查项 | 状态 |
|--------|------|
| 熔断器（circuit breaker，closed/open/half-open） | ✅ |
| 重试退避（retry + backoff） | ✅ |
| 截断续写 chatWithContinuation | ✅ |
| deepseek-v4-flash 模型 | ✅ |

### 维度 8：ModelGateway 多 Provider 路由 — ✅ 完整（Phase 5 已验证）

**位置**：[src/gateway/ModelGateway.ts](file:///d:/code/AIkefu/src/gateway/ModelGateway.ts)

| 检查项 | 状态 | 验证方式 |
|--------|------|----------|
| route/directRoute/cascade 实现 | ✅ | ModelGateway.ts route() |
| maxTokens/temperature/cascade 字段传递 | ✅ | types.ts:L84-L103 |
| DeepSeekProvider.capability.available 动态读取 circuit state | ✅ | DeepSeekProvider.ts:L25-L39 |
| 4 个调用点迁移到 gateway.route() | ✅ | Phase 5 F1-F12 验证全通过 |
| runTestReply 诊断路径保留 DeepSeekClient 直接调用 | ✅ | ShopSupervisor.ts:L2233 |

### AI 客服功能小结：**8/8 ✅ 完整**

---

## 二、改备注功能完整性审查（6 维度）

### 维度 1：触发入口 — ✅ 完整

**位置**：[ShopSupervisor.ts:1194-1208](file:///d:/code/AIkefu/src/shop/ShopSupervisor.ts#L1194)

```typescript
// 回复发送成功后延迟 1.5 秒触发备注
setTimeout(() => {
  this.executeOrderRemark(remarkContent).catch((err) => { ... });
}, 1500);
```

| 检查项 | 状态 |
|--------|------|
| 回复发送成功后触发 | ✅ |
| 1.5 秒延迟（避免与聊天框冲突） | ✅ |
| 异常捕获 | ✅ |

### 维度 2：备注页面打开（Alt+W） — ✅ 完整

**位置**：[WebviewClient.ts:2780-2784](file:///d:/code/AIkefu/src/cdp/WebviewClient.ts#L2780)

```typescript
// 步骤1：发送 Alt+W 快捷键打开订单备注页面
void this.webContents.sendInputEvent({ type: 'keyDown', keyCode: 'w', modifiers: ['alt'] });
void this.webContents.sendInputEvent({ type: 'keyUp', keyCode: 'w', modifiers: ['alt'] });
await new Promise((r) => setTimeout(r, 1500));
```

| 检查项 | 状态 |
|--------|------|
| Alt+W 快捷键 | ✅ |
| sendInputEvent 派发 keyDown/keyUp | ✅ |
| modifiers: ['alt'] | ✅ |
| 等待页面加载 1.5s | ✅ |

### 维度 3：输入框定位与清空 — ✅ 完整

**位置**：[WebviewClient.ts:2786-2899](file:///d:/code/AIkefu/src/cdp/WebviewClient.ts#L2786)

```typescript
// 步骤2：定位备注输入框（策略A：含"备注"文本的元素附近 / 策略B：所有 textarea 排除聊天框）
// 步骤3：sendInputEvent 模拟鼠标点击输入框
// 步骤4：Ctrl+A 全选 + Delete 删除
void this.webContents.sendInputEvent({ type: 'keyDown', keyCode: 'a', modifiers: ['control'] });
void this.webContents.sendInputEvent({ type: 'keyUp', keyCode: 'a', modifiers: ['control'] });
void this.webContents.sendInputEvent({ type: 'keyDown', keyCode: 'Delete' });
void this.webContents.sendInputEvent({ type: 'keyUp', keyCode: 'Delete' });
```

| 检查项 | 状态 |
|--------|------|
| 输入框定位策略A（"备注"文本附近） | ✅ |
| 输入框定位策略B（所有 textarea 排除聊天框） | ✅ |
| Ctrl+A（modifiers: ['control']）+ Delete 清空 | ✅ |

### 维度 4：文本输入（逐字 50ms） — ✅ 完整

**位置**：[WebviewClient.ts:2902-2907](file:///d:/code/AIkefu/src/cdp/WebviewClient.ts#L2902)

```typescript
// 步骤4：逐字输入备注内容（真实键盘事件）
for (const ch of remark) {
  void this.webContents.sendInputEvent({ type: 'char', keyCode: ch });
  await new Promise((r) => setTimeout(r, 50));  // 50ms/字
}
```

| 检查项 | 状态 |
|--------|------|
| 循环每个字符调用 sendInputEvent | ✅ |
| 50ms/字 延迟 | ✅ |
| type: 'char' 触发 React onChange | ✅ |

### 维度 5：保存与确认 — ✅ 完整（agent 误报，主代理交叉验证确认）

**位置**：[WebviewClient.ts:2909-2986](file:///d:/code/AIkefu/src/cdp/WebviewClient.ts#L2909)

```typescript
// 步骤5：定位并点击保存按钮（支持 4 种文本）
const saveBtnScript = `... if (t === '保存' || t === '确定' || t === '确认' || t === '提交') ...`;
if (saveBtnResult.found) {
  // 鼠标点击保存按钮
  void this.webContents.sendInputEvent({ type: 'mouseDown', x, y, ... });
  void this.webContents.sendInputEvent({ type: 'mouseUp', x, y, ... });
} else {
  // Fallback 1: Ctrl+Enter 提交
  void this.webContents.sendInputEvent({ type: 'keyDown', keyCode: 'Enter', modifiers: ['control'] });
  // Fallback 2: 失焦保存（点击空白区域）
  void this.webContents.sendInputEvent({ type: 'mouseDown', x: 10, y: 10, ... });
}

// 步骤6：关闭备注页面（Escape）
void this.webContents.sendInputEvent({ type: 'keyDown', keyCode: 'Escape' });
```

| 检查项 | 状态 |
|--------|------|
| 保存按钮点击（支持"保存"/"确定"/"确认"/"提交"） | ✅ |
| Ctrl+Enter fallback | ✅ |
| 失焦保存 fallback | ✅ |
| Escape 关闭页面 | ✅ |

### 维度 6：调用链与集成 — ✅ 完整

**位置**：[ShopSupervisor.ts:2087-2112](file:///d:/code/AIkefu/src/shop/ShopSupervisor.ts#L2087) `executeOrderRemark` 方法

| 检查项 | 状态 |
|--------|------|
| ShopSupervisor 调用 webviewClient.addOrderRemark | ✅ |
| 异常处理 try/catch + 日志记录 | ✅ |
| 备注失败不影响主回复 | ✅（async 异步执行） |

### 改备注功能小结：**6/6 ✅ 完整**（agent 误报维度 5，已交叉验证修复）

---

## 三、识别买家询问的商品功能完整性审查（7 维度）

### 维度 1：detectOrderInfo 两阶段策略 — ✅ 完整（agent 误报，主代理交叉验证确认）

**位置**：[WebviewClient.ts:3337-3415](file:///d:/code/AIkefu/src/cdp/WebviewClient.ts#L3337)

```javascript
async detectOrderInfo(): Promise<string> {
  // 步骤1：点击"订单"标签页
  // 步骤2：提取右侧面板订单信息
  
  // 策略A：查找包含"订单号"/"订单编号"关键词的元素，提取其父容器的文本
  if (t.indexOf('订单号') >= 0 || t.indexOf('订单编号') >= 0) {
    var container = el.parentElement;
    for (var depth = 0; depth < 5 && container; depth++) {
      var containerText = (container.innerText || container.textContent || '').trim();
      if (containerText.length > 30 && containerText.length < 1500) {
        fullText = containerText; break;
      }
      container = container.parentElement;
    }
  }
  
  // 策略B：如果策略A没找到，回退到查找右侧面板中含"订单"/"商品"/"实付"的最长文本
  if (!fullText) {
    if (text.indexOf('订单') >= 0 || text.indexOf('商品') >= 0 || text.indexOf('实付') >= 0) {
      panelTexts.push(text);
    }
  }
}
```

| 检查项 | 状态 |
|--------|------|
| 策略A：含"订单号"/"订单编号"的元素提取父容器文本 | ✅ |
| 策略B：含"订单"/"商品"/"实付"的最长文本 fallback | ✅ |
| 右侧面板限制（isInRightPanel） | ✅ |
| 可见性过滤（isVisible） | ✅ |

### 维度 2：ProductMatcher 三级匹配 — ✅ 完整

**位置**：[src/product/ProductMatcher.ts:72-152](file:///d:/code/AIkefu/src/product/ProductMatcher.ts#L72)

```typescript
match(text: string): MatchResult[] {
  // 1. 关键词匹配
  for (const [kw, ids] of this.keywordIndex) {
    if (lower.includes(kw)) { ... matchType: 'keyword' }
  }
  // 2. 精确匹配商品名
  for (const p of this.products) {
    if (text.includes(p.name)) { ... matchType: 'exact', confidence: 1.0 }
  }
  // 3. 模糊匹配（Levenshtein 距离）
  if (algo === 'fuzzy' || algo === 'hybrid') {
    const sim = this.similarity(lower, c);  // L127-152 Levenshtein
    if (bestSim >= this.config.product.fuzzy_threshold) { ... matchType: 'fuzzy' }
  }
  return Array.from(results.values()).sort((a, b) => b.confidence - a.confidence);
}
```

| 检查项 | 状态 |
|--------|------|
| 关键词匹配（matchType: 'keyword'） | ✅ |
| 精确匹配（matchType: 'exact', confidence: 1.0） | ✅ |
| 模糊匹配 Levenshtein（matchType: 'fuzzy'） | ✅ |
| fuzzy_threshold 阈值控制 | ✅ |
| 结果按 confidence 排序 | ✅ |

### 维度 3：商品同步 ProductSyncService — ✅ 完整

**位置**：[src/product/ProductSyncService.ts](file:///d:/code/AIkefu/src/product/ProductSyncService.ts)

| 检查项 | 状态 |
|--------|------|
| API 拦截机制（fetch/XHR hook） | ✅ |
| Fallback：performance.getEntriesByType('resource') | ✅ |
| DOM 抓取 SCRAPE_PRODUCT_LIST_SCRIPT | ✅ |
| DOM 抓取 SCRAPE_PRODUCT_DETAIL_SCRIPT | ✅ |
| 仅同步"上架"状态商品 | ✅ |
| 保留 shipping/after_sales/faq 手动配置 | ✅ |
| 数据校验（名称≤200、ID有效、价格/库存完整） | ✅ |
| failedProducts 数组记录失败项 | ✅ |

### 维度 4：parseItem 字段提取 — ✅ 完整（agent 误报缺失，主代理交叉验证确认）

**位置**：[ProductSyncService.ts:942-971](file:///d:/code/AIkefu/src/product/ProductSyncService.ts#L942) （嵌入在 SCRAPE_PRODUCT_DETAIL_SCRIPT 字符串内）

```javascript
function parseItem(el) {
  var images = extractImages(el);  // ← memory 硬约束：先调用 extractImages 存入变量
  return {
    productId, name, sku, price, stock, url,
    imageUrl: images.length > 0 ? images[0] : '',  // ← 再用 images[0] 设置
    images: images,
    description: '',
    category: '',
    specs: [], attrs: [],
    active: isActiveStatus(rawText),
    status: status,
    sales: extractSales(rawText)
  };
}
```

| 检查项 | 状态 |
|--------|------|
| imageUrl 先 extractImages 存变量再用 images[0] | ✅ |
| category 字段（默认空字符串） | ✅ |
| 全字段提取（productId/name/sku/price/stock/url/imageUrl/images/specs/attrs/status/sales） | ✅ |
| 嵌入在脚本字符串内（注入页面执行） | ✅ |

### 维度 5：图片理解能力 — ✅ 完整

**位置**：[src/deepseek/DeepSeekClient.ts:144-155](file:///d:/code/AIkefu/src/deepseek/DeepSeekClient.ts#L144)

```typescript
// 多模态：user 消息带图片时转为 OpenAI 兼容的 content 数组格式（DeepSeek V4 Vision）
const apiMessages = req.messages.map((m) => {
  if (m.images && m.images.length > 0 && m.role === 'user') {
    const content = [
      { type: 'text', text: m.content },
      ...m.images.map((url) => ({ type: 'image_url', image_url: { url } })),
    ];
    return { ...m, content };
  }
  return m;
});
```

| 检查项 | 状态 |
|--------|------|
| DeepSeek V4 Vision 多模态 content 数组 | ✅ |
| 检测 m.images 提取 URL | ✅ |
| OpenAI 兼容格式（type: 'image_url'） | ✅ |

### 维度 6：商品上下文注入 — ✅ 完整（agent 误报缺失，主代理交叉验证确认）

**位置**：[ShopSupervisor.ts:1533-1659](file:///d:/code/AIkefu/src/shop/ShopSupervisor.ts#L1533) `generateReply` 方法

```typescript
// 3. 商品匹配（用于注入上下文）
let productContext = '';
if (this.productMatcher && this.deps.config.product.match_algorithm !== 'exact') {
  const matches = this.productMatcher.match(text);
  if (matches.length > 0) {
    const top = matches[0];
    session.productId = top.productId;
    const product = this.productMatcher.getProduct(top.productId);
    if (product) {
      productContext = this.buildProductContext(product);  // ← 商品知识注入
    }
  }
}

// 3b. 订单备注请求检测：注入订单信息
if ((isRemarkRequest || isRemarkConfirm) && this.webviewClient) {
  const orderInfo = await this.webviewClient.detectOrderInfo();
  if (orderInfo) {
    productContext += '\n\n当前会话订单信息：\n' + orderInfo;
  }
}

// 3a. 购买意图 + 推荐
if (this.conversionEngine && this.deps.config.conversion.enabled) {
  const recs = this.conversionEngine.recommend(...);
  productContext += this.buildRecommendationContext(recs);
}

// 跨会话买家画像注入（先认人再讲货）
let buyerContext = '';
if (this.buyerProfileService) {
  buyerContext = this.buyerProfileService.buildBuyerContext(...);
}

const agentCtx: AgentContext = {
  productContext,    // ← 商品上下文
  buyerContext,     // ← 买家画像
  knowledgeContext, // ← 模板知识
  ...
};
```

| 检查项 | 状态 |
|--------|------|
| ProductMatcher.match() 调用 | ✅ L1535 |
| buildProductContext 商品知识构建 | ✅ L1541 |
| detectOrderInfo 订单信息注入 | ✅ L1559 |
| 推荐上下文 buildRecommendationContext | ✅ L1596 |
| buyerContext 买家画像注入（先认人再讲货） | ✅ L1638-1652 |
| AgentContext 包含 productContext/buyerContext/knowledgeContext | ✅ L1654-1670 |
| section 顺序符合 memory 硬约束 | ✅（角色→知识边界→回复规范→当前上下文→买家画像→商品知识→特殊指令） |

### 维度 7：买家询问识别（运行时） — ✅ 完整

**位置**：[src/intent/LlmIntentRecognizer.ts:41-121](file:///d:/code/AIkefu/src/intent/LlmIntentRecognizer.ts#L41) + [ShopSupervisor.ts:1535](file:///d:/code/AIkefu/src/shop/ShopSupervisor.ts#L1535)

| 检查项 | 状态 |
|--------|------|
| LlmIntentRecognizer.recognize() 提取 entities | ✅ |
| entities 包含 product_name 槽位 | ✅ |
| ProductMatcher.match(text) 与消息内容直接匹配 | ✅ |
| 匹配结果写入 session.productId | ✅ L1538 |
| top 商品通过 getProduct 注入上下文 | ✅ L1539-1541 |

### 商品识别功能小结：**7/7 ✅ 完整**（agent 误报 3 项缺失，已交叉验证全部存在）

---

## 四、汇总报告

### 总体评分

| 功能模块 | 维度数 | 通过 | 完整率 |
|----------|--------|------|--------|
| AI 客服功能 | 8 | 8 | 100% ✅ |
| 改备注功能 | 6 | 6 | 100% ✅ |
| 商品识别功能 | 7 | 7 | 100% ✅ |
| **合计** | **21** | **21** | **100% ✅** |

### 与 memory 硬约束的符合度

#### AI 客服硬约束
- ✅ ShopSupervisor.handleIncomingMessage 检查 autoReply 状态
- ✅ ShopSupervisor.runVisualPoll 检查 autoReply 状态
- ✅ ShopInstance 维护 processingSessions + sessionCooldowns
- ✅ 敏感词库 145 条（block=91, warn=54）
- ✅ "微信"/"支付宝"/"线下" 为 block 类型
- ✅ DeepSeek 使用 deepseek-v4-flash
- ✅ 7 级管线顺序正确
- ✅ ModelGateway 4 个调用点已迁移

#### 改备注硬约束
- ✅ Alt+W 快捷键打开
- ✅ sendInputEvent 模拟真实键盘输入
- ✅ 回复发送成功后延迟 1.5 秒
- ✅ Ctrl+A + Delete 清空
- ✅ 50ms/字 逐字输入
- ✅ 保存按钮点击 + Ctrl+Enter + 失焦保存 fallback

#### 商品识别硬约束
- ✅ detectOrderInfo 两阶段策略
- ✅ ProductMatcher 三级匹配（关键词→精确→模糊 Levenshtein）
- ✅ 商品同步 API 优先 + DOM fallback
- ✅ parseItem imageUrl 先 extractImages 再 images[0]
- ✅ 仅同步"上架"状态商品
- ✅ 商品上下文注入 generateReply
- ✅ 买家画像注入（先认人再讲货）
- ✅ DeepSeek V4 Vision 多模态

### Agent 误报项汇总（主代理交叉验证修复）

| Agent | 误报维度 | 误报原因 | 实际状态 |
|-------|----------|----------|----------|
| Agent 1（AI 客服） | 维度 8 ModelGateway "部分完整" | 未读取 Phase 5 验证结果 | ✅ 完整（4 调用点已迁移） |
| Agent 2（改备注） | 维度 5 "保存与确认部分完整" | 未深入读取 addOrderRemark 步骤 5-6 | ✅ 完整（保存按钮 + Ctrl+Enter + 失焦保存） |
| Agent 3（商品识别） | 维度 1 detectOrderInfo "部分完整" | 搜索不彻底，未找到 WebviewClient.ts:3337 | ✅ 完整（两阶段策略实现） |
| Agent 3（商品识别） | 维度 4 parseItem "缺失" | 未识别嵌入在 SCRAPE 脚本字符串内 | ✅ 完整（L942-971） |
| Agent 3（商品识别） | 维度 6 商品上下文注入 "缺失" | 未读 ShopSupervisor.ts L1533-1659 | ✅ 完整（productMatcher + buildProductContext） |

### 高优先级问题清单

**无高优先级问题。** 所有 21 个维度全部通过，代码完整，功能完整。

### 后续建议（非阻塞）

1. **运行时回归测试**：建议执行 F1-F12 CDP 验证脚本（`scripts/test-multi-provider-effective.cjs`）确认运行时无回归
2. **手动场景测试**：可选执行 S1（多轮上下文记忆）+ S4（转人工触发）+ S5（买家画像注入）核心场景验证
3. **商品识别集成度**：考虑在 LlmIntentRecognizer 中显式调用 ProductMatcher，目前 ProductMatcher 是在 ShopSupervisor.generateReply 中独立调用，集成度可进一步优化

---

## 五、检查方法说明

### 审查流程
1. 主代理创建 3 个并行 Explore agent，每个负责一个功能模块
2. agent 返回初步审查结果
3. 主代理对 agent 误报的"缺失/部分完整"项，亲自读取源码交叉验证
4. 主代理汇总并生成最终报告

### 工具使用
- Agent (Explore subagent_type) × 3 并行
- Read 直接读取关键文件做交叉验证
- Grep/rg 搜索关键词定位代码位置

### 报告生成
- 2026-07-19 完成审查
- 报告路径：[docs/AI客服三大功能逐帧检查报告-2026-07-19.md](file:///d:/code/AIkefu/docs/AI客服三大功能逐帧检查报告-2026-07-19.md)
